# Ansen

Static anime page generator.

## Usage

1. Install dependencies:

```bash
npm install
```

2. Update `data/animes.json` with anime and episode data.

3. Generate pages:

```bash
npm run generate
```

4. Generated pages are written to:

- `public/anime`
- `public/episodio`
